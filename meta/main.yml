galaxy_info:
  author: Kadaan
  description: Installs zsh and configures it as the default shell.
  license: Apache
  min_ansible_version: 1.2
  platforms:
    - name: MacOSX
      versions:
        - 10.11
        - 10.12
  galaxy_tags:
    - zsh
dependencies:
  - geerlingguy.homebrew
  # List your role dependencies here, one per line.
  # Be sure to remove the '[]' above if you add dependencies
  # to this list.
